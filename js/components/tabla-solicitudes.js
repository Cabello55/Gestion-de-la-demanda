// Tabla (stub) para solicitudes.

export function renderTablaSolicitudes(_solicitudes = []) {
  return `
    <div class="muted">
      Tabla de solicitudes (stub). En la siguiente iteración rellenaremos estructura, filtros y acciones.
    </div>
  `;
}

